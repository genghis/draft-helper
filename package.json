{
  "name": "drafthelper",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build",
    "synth": "pnpm --filter @drafthelper/infra synth"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
